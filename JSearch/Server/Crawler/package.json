{
  "name": "crawler",
  "version": "1.0.0",
  "description": "crawl job opnenin and link data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "set NODE_ENV=development && nodemon ./index.js",
    "prod": "set NODE_ENV=production && node ./index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1527.0",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.3"
  }
}
